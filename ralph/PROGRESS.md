# PROGRESS.md — Ralph loop log

## 2026-07-08T12:00:00+02:00 — T-00: PASS
- What changed: Recon only. Wrote `ralph/FINDINGS.md` answering every T-00 bullet: call stack is **LiveKit** (CallPage.tsx + relay token endpoint :8788); `vendor/gossip` **is** vendored and gossip-sdk **does** expose `editMessage`/`deleteMessage`/`sendReaction` (T-10 unblocked) but has **no attachment API** (T-13 DM part blocked); DMs are **real SDK** (RealDmView), channels real via relay WS; DM + channel composers are **separate** components (not shared — affects T-01/02/03); design system is **Stack monochrome** (`--st-*` in packages/ui/src/stack/tokens.css), not the "dark + mint" the brief assumed.
- Files: ralph/FINDINGS.md, ralph/RALPH.md (checkbox), ralph/PROGRESS.md, ralph/CHECKUP.md (copied in)  (left UNCOMMITTED in the working tree)
- Checkup: A1 ✔ (no product code touched — typecheck/lint/build baseline unaffected, not run) A2 n/a (no UI change) A3 ✔ (HEAD 1a47387 unchanged; diff ralph/-only; no secrets; no vendor/ edits) A4 ✔ | B(T-00) ✔ (all bullets answered with real paths; call stack unambiguous; SDK APIs named; no product code changed)
- Notes / follow-ups / flags for Daniel:
  - `.gitignore` was NOT touched (T-00 acceptance = no changes outside ralph/). Next task should add `ralph/.ralph_*` to `.gitignore` as part of §3 bookkeeping.
  - RALPH.md task briefs say "dark + mint" but the live styleguide is Stack monochrome — new UI will follow **Stack** per your styleguide directive.
  - T-10 is implementable (SDK has edit/delete); T-13 DM attachments will be blocked (no SDK API).

## 2026-07-08T12:35:00+02:00 — T-01: PASS
- What changed: Emoji picker in the composer. Added `frimousse` (headless picker) + `@fontsource/noto-color-emoji` (self-hosted consistent glyph set). New `EmojiPickerPopover.tsx` styled with Stack tokens (paper/line/field/ink, rounded-card, Esc + outside-click close, keyboard-navigable via frimousse). `Composer.tsx` gained a Smile toggle in the toolbar, insert-at-cursor with caret restore, and optional `busy` prop. **Unified composers:** `RealDmView.tsx` now uses the shared `Composer` (its inline input removed) — so the picker works in both DMs and channels, and future composer features (T-02/T-03) land in one place. Emoji font appended to `--font-sans` (app) and `--font-stack` (Stack tokens) so sent messages render the same set. Also added `ralph/.ralph_*` to `.gitignore` (§3 bookkeeping deferred from T-00).
- Files: apps/web/src/components/chat/EmojiPickerPopover.tsx (new), Composer.tsx, RealDmView.tsx, apps/web/src/index.css, packages/ui/src/stack/tokens.css, apps/web/package.json, pnpm-lock.yaml, .gitignore  (left UNCOMMITTED in the working tree)
- Checkup: A1 ✔ (web typecheck clean; web build ✓; root turbo typecheck fails only on **pre-existing** vendor/gossip `copy:wasm` Windows incompatibility — untouched baseline; web has no lint script) A2 ✔/eyeball (dev server boots, `/` and `/dev/styleguide` return 200, compiled CSS contains the emoji font + `.font-emoji`; **needs human eyeball:** picker opens in DM + channel, cursor insertion, glyphs are Noto not OS set, no console errors) A3 ✔ (HEAD unchanged; vendor/ clean; no secrets; lockfile churn = intentional dep add) A4 ✔ | B(T-01) ✔ pending eyeball items above
- Notes / follow-ups / flags for Daniel:
  - frimousse fetches its emoji **metadata** from Emojibase's CDN at runtime by default (glyph rendering itself is the self-hosted font). If you want zero external fetches, it supports a self-hosted `emojibaseUrl` — say the word and a later run can vendor `emojibase-data`.
  - The DM composer now shows the shared Composer's fuller footer ("End-to-end encrypted" + toolbar) instead of the old compact inline pill — intentional consolidation, flag if you preferred the compact one.
  - Root `pnpm typecheck`/`build`/`lint` (turbo) cannot pass on Windows because vendor/gossip's `copy:wasm` uses `mkdir -p`/`cp` — pre-existing, not from this loop. Per-package web commands used instead.

## 2026-07-08T13:10:00+02:00 — T-02: PASS
- What changed: Markdown formatting (bold/italic/inline code/fenced code blocks). New `MessageBody.tsx` — a zero-dependency renderer that builds **React elements only** (never parses HTML, no links auto-rendered, no `dangerouslySetInnerHTML`), so `<img onerror=…>` / `[x](javascript:…)` render as inert literal text — verified by an actual `renderToStaticMarkup` test run (9 cases incl. 3 XSS probes + nested bold/italic). Storage stays plain markdown text (transport-agnostic, E2E-safe). `Composer.tsx` gained B/I/code/code-block toolbar buttons (mousedown-preventDefault so selection survives) with wrap/unwrap toggling + Ctrl/Cmd-B, -I, -E shortcuts. Rendering wired into all three surfaces: `RealDmView` (DM), `ChannelView` (channel), `ThreadPanel` (thread replies). `.md-chip` utility (color-mix on currentColor) keeps code chips legible on both ink-on-paper and paper-on-ink bubbles in both themes.
- Files: apps/web/src/components/chat/MessageBody.tsx (new), Composer.tsx, RealDmView.tsx, ThreadPanel.tsx, apps/web/src/pages/ChannelView.tsx, apps/web/src/index.css  (left UNCOMMITTED in the working tree)
- Checkup: A1 ✔ (web typecheck ✓, web build ✓; no new deps; turbo root fails only on pre-existing vendor Windows issue) A2 ✔/eyeball (dev boots; `/`, `/dev/styleguide`, MessageBody transform all 200; `.md-chip` present in compiled CSS; **needs human eyeball:** visual look of chips/blocks in both surfaces + shortcuts feel) A3 ✔ (HEAD unchanged; vendor clean; no secrets; no lockfile churn this task) A4 ✔ (T-01 changes untouched and still present) | B(T-02) ✔ — XSS checks passed via real render test, not just inspection
- Notes / follow-ups / flags for Daniel:
  - Renderer intentionally supports exactly what the toolbar produces. No links/headings/lists — literal text. Say the word if you want autolinked URLs later (needs protocol allow-listing).
  - Ctrl+E = inline code (Slack convention). Code-block button wraps selection in ``` fences.

## 2026-07-08T13:40:00+02:00 — T-03: PASS
- What changed: "+" more-options popover in the shared composer (so it appears in both DM and channel composers automatically). New `ComposerPlusMenu.tsx`: branded menu (Stack tokens), focus moves in on open, ArrowUp/Down/Home/End roving focus, **Tab trapped** inside, Esc + outside-click close, focus returns to the textarea. "Attach file or media" opens the **native OS file picker** (hidden `<input type=file multiple>`); since T-13 isn't built, picking files shows an honest info toast — "Attachments coming soon — nothing was uploaded." — and does nothing else (no fake success). "Code snippet" and "Poll" are disabled placeholder rows labelled "Soon". `StackToast` in packages/ui gained an additive `tone?: "success" | "info"` prop (default unchanged → styleguide unaffected); the no-op notice uses `tone="info"` so it can't read as a success confirmation.
- Files: apps/web/src/components/chat/ComposerPlusMenu.tsx (new), Composer.tsx, packages/ui/src/stack/settings/SettingsDanger.tsx  (left UNCOMMITTED in the working tree)
- Checkup: A1 ✔ (typecheck 0 errors, build ✓, no new deps) A2 ✔/eyeball (dev boots; `/`, `/dev/styleguide`, new component transform all 200; **needs human eyeball:** popover look, focus trap feel, OS picker opens, toast appears) A3 ✔ (HEAD unchanged; vendor clean; secret-grep clean — "token" hits were the tokens.css filename) A4 ✔ (T-01/T-02 work untouched) | B(T-03) ✔ — attach = real OS picker + visible no-op, not faked
- Notes / follow-ups / flags for Daniel:
  - T-13 should replace the `onAttach` no-op in Composer.tsx with the real upload flow — it receives the `FileList` already.
  - The "+" icon rotates 45° when open (reads as ×) — tell me if too cute.

## 2026-07-08T14:05:00+02:00 — T-04: PASS
- What changed: Per-message **Copy** + **Share** hover actions in both surfaces. New `MessageActionsBar.tsx` (Stack-styled pill, copy feedback flips icon to a ✓ for 1.5s). **Channels:** the existing hover "Reply" pill was absorbed into the bar (Reply · Copy · Share, same position/styling). **DMs:** bar appears beside the bubble on hover (left of own messages, right of the peer's). Copy = raw message text. Share = quoted text + attribution; in channels it appends the **real** channel URL (`/w/:id/c/:id` — an existing route); per-message deep links don't exist, so no route was invented — text fallback per the task brief. No storage/transport changes (useRelay/gossip-sdk untouched).
- Files: apps/web/src/components/chat/MessageActionsBar.tsx (new), RealDmView.tsx, apps/web/src/pages/ChannelView.tsx  (left UNCOMMITTED in the working tree)
- Checkup: A1 ✔ (typecheck 0 errors, build ✓) A2 ✔/eyeball (dev boots, all routes 200; **needs human eyeball:** hover reveal, clipboard content, ✓ feedback) A3 ✔ (HEAD unchanged; vendor clean; diff render-layer only) A4 ✔ | B(T-04) ✔ — no invented routes, no transport diff
- Notes / follow-ups / flags for Daniel:
  - Share uses clipboard (deterministic) rather than the native `navigator.share` sheet — easy to switch if you prefer the OS share dialog.
  - When T-09/T-10 (edit/delete) land they should slot into this same bar.

## 2026-07-08T14:55:00+02:00 — T-05: PASS
- What changed: Avatar system. **DiceBear identicon** (dev/hacker GitHub-style) seeded deterministically from the user's handle — verified by a real node run (same seed → identical data-URI; different seed → distinct). New `lib/avatar.ts` (cached generator, `randomSeed`, `fileToAvatarDataUrl` with type check, 2 MB cap, cover-crop + downscale to 128px webp via canvas). New persisted Zustand store `useAvatars` (localStorage `gossip-avatars`) holding per-handle overrides (reseed or custom image). Stack `Avatar` gained an additive `src` prop (initials fallback unchanged → styleguide untouched). New `UserAvatar` wrapper resolves override > seed > deterministic-default; **aliased into all 9 app call sites** via import swap (`UserAvatar as Avatar`) so JSX stayed identical. ProfileSettings got an Avatar section: preview-before-save Regenerate ("Try another" cycles), Upload with validation + downscale, Reset to default. Deps: `@dicebear/core@9` + `@dicebear/collection@9` (had to pin core to 9 — pnpm resolved core@10 which breaks collection@9's `escape` import; caught by the build).
- Files: apps/web/src/lib/avatar.ts, stores/useAvatars.ts, components/UserAvatar.tsx (new); packages/ui/src/stack/workspace/Avatar.tsx (+src prop); import swaps in ChannelSidebar, WorkspaceRail, MembersPage, Threads, WorkspaceSettings, ProfileSettings, RealDmView, ThreadPanel, ChannelView; apps/web/package.json, pnpm-lock.yaml  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓ after core@9 pin) A2 ✔/eyeball (dev boots on 5173, styleguide 200 & visually unaffected — it uses the initials Avatar directly; **needs human eyeball:** identicon look across sidebar/messages/members, regenerate/upload flow, reload persistence) A3 ✔ (HEAD unchanged; vendor clean; avatars stored in localStorage only — nothing leaves the device) A4 ✔ | B(T-05) ✔ — determinism verified programmatically; persistence via zustand/persist; no Drizzle needed (app has no own DB; SDK schema is sealed vendor)
- Notes / follow-ups / flags for Daniel:
  - **Sync gap (by design for now):** your custom upload/reseed is local-only — peers see your deterministic identicon until the relay member protocol carries avatar overrides. Flagged, not faked.
  - Persistence is localStorage (no app-side Drizzle DB exists; the CLAUDE.md §9 workspace schema isn't built yet). When it lands, migrate `gossip-avatars` in.
  - Ops note: several orphaned vite dev servers from earlier iterations were found listening on 5173-5177 (TaskStop doesn't kill the vite child on Windows) — all killed; later smoke tests now verify a fresh 5173.

## 2026-07-08T15:25:00+02:00 — T-06: PASS
- What changed: Contact sharing. **Link:** `/contact/:handle?name=…` — new top-level route (task brief sanctioned defining one; modeled on the existing `/join/:code` pattern) carrying only the **public** handle + optional display name. **Share surface:** ProfileSettings "Share my contact" — QR code (`qrcode.react`, fixed black-on-white card for scannability in both themes), copyable link with ✓ feedback, native `navigator.share` button where supported. **Inbound:** `ContactLanding.tsx` validates the handle (`gossip1…` regex), rejects invalid links, recognizes your own link, and with an open session shows a **start-DM confirmation** (avatar + name + handle) → adds contact via `useContacts.add` → navigates to the real DM route `/w/:workspaceId/dm/:handle`. Locked session → contact is stashed in sessionStorage (single-use `takePendingContact`) and the user detours through unlock/create; `AppShell` consumes the stash once unlocked-and-in-a-workspace and opens the DM. No workspace at all → stash kept, user routed to onboarding.
- Files: apps/web/src/lib/contact.ts, pages/ContactLanding.tsx (new); router.tsx (+route), app/AppShell.tsx (pending-contact consumption), pages/settings/ProfileSettings.tsx (share UI); package.json + lockfile (qrcode.react)  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔/eyeball (dev boots fresh on 5173; `/contact/<handle>` 200; styleguide 200; **needs human eyeball:** QR scans, confirm flow, locked-session detour end-to-end with a second identity) A3 ✔ (HEAD unchanged; vendor clean; link carries public handle only) A4 ✔ | B(T-06) ✔ — real DM route reused, nothing secret shared
- Notes / follow-ups / flags for Daniel:
  - The QR encodes the full URL (origin-dependent) — on the deployed origin it'll encode that origin automatically.
  - True end-to-end test needs two identities + the relay running (`services/relay`, port 8788 per vite proxy).

## 2026-07-08T15:50:00+02:00 — T-07: PASS (LiveKit path per FINDINGS)
- What changed: Call UI restyled to brand. The prebuilt `<VideoConference/>` is themed by overriding LiveKit's **`--lk-*` design tokens** with Stack tokens in index.css (doubled `[data-lk-theme="default"][data-lk-theme="default"]` selector to out-specify @livekit/components-styles, which loads later): paper/field/line surfaces, ink foregrounds, **monochrome ink-on-paper accent** (matches Stack buttons), `--st-negative` danger, Inter font, 0.5rem control radius, connection-quality colors from positive/negative. Because values reference `--st-*` vars, **dark mode follows automatically**. CallPage's hard-coded `#0a0c0f` background replaced with `bg-paper font-stack`. Kept the prebuilt component (controls/tiles/tray/device-menus keep working) rather than a risky from-scratch rebuild in an unattended loop.
- Files: apps/web/src/index.css (theme block), apps/web/src/pages/CallPage.tsx (container classes)  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔/eyeball (dev boots; compiled CSS contains the override block; styleguide untouched — override is scoped to [data-lk-theme]; **needs human eyeball:** join a real call and confirm controls/tiles look Stack-branded in light + dark) A3 ✔ (HEAD unchanged; vendor clean) A4 ✔ | B(T-07) ✔ — LiveKit path taken, no iframe limitation applies
- Notes / follow-ups / flags for Daniel:
  - This is CSS-token theming of the prebuilt UI. If you want a fully custom tray/tile layout (T-14 will already add a custom dock), that's a follow-up with LiveKit hooks (`GridLayout`/`ControlBar`).
  - Needs a LiveKit project configured in `services/relay/.env` to see it live.

## 2026-07-08T16:15:00+02:00 — T-08: PASS
- What changed: 1:1 calls from DMs. Call target generalized: new route `/w/:workspaceId/call/dm/:peerId` alongside the untouched channel route (`call/:channelId` — channel calls regression-safe: same room string `${workspaceId}:${channelId}`, same token flow). DM rooms derived via new `lib/call.ts` `dmRoomName()` — **SHA-256 of the sorted handle pair**, so both participants independently derive the same room and the name is an opaque digest (verified by node run: symmetric ✓, no handle substrings ✓). RealDmView header gained a Phone button (hidden for Notes-to-Self) → the DM call route. CallPage now titles "Call · <peer>" vs "Huddle · #channel", back-navigates to the right surface, and requires an open session for DM calls (clear error otherwise). Reuses the themed LiveKit surface from T-07.
- Files: apps/web/src/lib/call.ts (new), pages/CallPage.tsx, components/chat/RealDmView.tsx, app/router.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔/eyeball (dev boots; both call routes 200; **needs human eyeball:** two-identity call join both directions) A3 ✔ (HEAD unchanged; vendor clean; room names carry zero DM content — hashed pair only) A4 ✔ | B(T-08) ✔ — channel path untouched, opaque ids verified programmatically
- Notes / follow-ups / flags for Daniel:
  - The relay's /livekit-token endpoint will now see `dm:<digest>` rooms — if it ever validates room formats, allow that prefix.
  - No ring/notify yet: the peer must open the call from their side (call button in the DM). A "incoming call" signal needs relay support — out of this task's scope.

## 2026-07-08T16:55:00+02:00 — T-09: PASS
- What changed: Channel message **edit + delete**, end to end. **Relay** (`services/relay/server.mjs`): new `editMessage` (author-only; sets `editedAt`, re-fans-out as `messageUpdated`) and `deleteMessage` (author **or** workspace admin/owner per member role; soft delete — `deleted:true`, body cleared, `deletedAt`/`deletedBy`; tombstone stays in history so it persists across reload). **Store** (`useRelay`): `ChannelMsg` gained `editedAt`/`deleted`/... fields (additive); `messageUpdated` handler replaces in place; `editMessage`/`deleteMessage` actions. **UI** (`ChannelView`): hover bar now shows Edit (own messages → inline EditBox, Enter=save/Esc=cancel) and a two-step **ArmDeleteButton** (first click arms red, auto-disarms 2.5s — no accidental deletes, no ugly window.confirm); "(edited)" marker; "message deleted" tombstone; actions hidden on tombstones. ThreadPanel rows render tombstones/edited markers too. Channels aren't Drizzle-backed (relay + .data.json is the store of record — CLAUDE.md §9 schema doesn't exist in-app yet), so "additive migration" = additive message fields; old messages parse fine.
- Files: services/relay/server.mjs, apps/web/src/stores/useRelay.ts, pages/ChannelView.tsx, components/chat/ThreadPanel.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔ (relay boots) A3 ✔ (HEAD unchanged; vendor clean) A4 ✔ | **B(T-09) ✔ verified by a real 2-client WS E2E test** against a throwaway relay (isolated DATA_DIR, port 8799): author edit + editedAt ✓ · non-author edit REJECTED ✓ · non-admin delete REJECTED ✓ · owner deletes another's message ✓ · body cleared ✓ · tombstone persists in re-fetched history ✓ (7/7 PASS)
- Notes / follow-ups / flags for Daniel:
  - Restart your local relay to pick up the new ops (`node services/relay/server.mjs`).
  - Deployed relay (fly.toml) needs a redeploy before edit/delete works in prod.
  - UI eyeball: edit box styling, armed-delete red state, "(edited)" marker.

## 2026-07-08T17:30:00+02:00 — T-10: PASS (SDK-backed — NOT blocked)
- What changed: DM message edit + delete via **gossip-sdk only** (the RALPH.md §4 assumption that this needed a missing API was wrong — FINDINGS T-00 located `messages.editMessage(id, body)` / `messages.deleteMessage(id)`). Both send real **E2E control messages** to the peer (`editOf`/`deleteOf` protocol fields, crypto boundary untouched — zero vendor/ changes, verified). The SDK enforces author-only edits (`direction !== OUTGOING → false`) and mirrors the local DB itself; the view refreshes via the already-subscribed `MESSAGE_UPDATED`/`MESSAGE_DELETED` events. UI: Edit (pencil → inline EditBox) + armed Delete on your own DM bubbles; deleted messages render an in-bubble italic tombstone (SDK stores `type: "deleted"`); "(edited)" marker read from `metadata.edited` (set by the SDK). Not offered on Notes-to-Self (separate selfMessages service without edit API — honest omission, not faked). Shared `EditBox`/`ArmDeleteButton` extracted to MessageActionsBar.tsx and reused by ChannelView (dedup from T-09).
- Files: apps/web/src/components/chat/RealDmView.tsx, MessageActionsBar.tsx (+2 exported components), pages/ChannelView.tsx (dedup), apps/web/src/lib/sdk.ts (re-export MessageType)  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔/eyeball (dev boots; transforms 200; **needs human eyeball:** edit/delete round-trip between two identities — edited/tombstone should appear on BOTH sides) A3 ✔ (HEAD unchanged; vendor/gossip 0 changes — SDK used as-is; no crypto bypass) A4 ✔ | B(T-10) ✔ — SDK path, local state mirrors via SDK events
- Notes / follow-ups / flags for Daniel:
  - Peer-side rendering of an *incoming* edit/delete depends on the SDK applying the control message to the local DB (it does — `performEditMessage`/`PerformDeleteMessage` handle inbound controls); please verify visually in your two-browser setup.
  - Notes-to-Self edit/delete = SDK gap (selfMessages has no mutation API); flagged, not faked.

## 2026-07-08T18:05:00+02:00 — T-11: PASS (verified against the real local model)
- What changed: "Improve draft" AI rewrite, privacy-pinned to the local model. **Bridge** (`packages/openclaw-bridge`, the mandated boundary): additive `RewriteRequest`/`RewriteResult` types where `route` is the literal type `"local"` — cloud is unrepresentable at the type level — plus `client.rewriteDraft()`. **Gateway** (relay `/openclaw/rewrite`): refuses any non-local route (tested: `route:"cloud"` → 400 "Draft rewriting is local-route only"), rejects empty drafts, never logs or persists the draft, forwards only to local Ollama. **App**: singleton bridge client (`lib/openclaw.ts` — AI now flows through the package boundary), wand button in the shared composer (DM + channel), suggestion panel above the composer ("local model" badge, **Use this** replaces the draft / **Try again** / **X dismiss** leaves the draft untouched — stated inline). Only the unsent draft is ever sent; received messages are never read. **Live-tested against the actual Ollama qwen2.5:7b on this machine:** first prompt version made the model *answer* the draft; fixed with `<<<`/`>>>` draft markers + "not addressed to you" instruction — retest returned a faithful polished rewrite.
- Files: packages/openclaw-bridge/src/types.ts + client.ts, services/relay/server.mjs (+/openclaw/rewrite), apps/web/src/lib/openclaw.ts (new), components/chat/Composer.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔ A3 ✔ (HEAD unchanged; vendor clean; **no keys in frontend** — grep clean; DM drafts cannot hit cloud: refused server-side AND unrepresentable in the bridge types) A4 ✔ | B(T-11) ✔ — end-to-end verified with the real local model, including both privacy refusals
- Notes / follow-ups / flags for Daniel:
  - Restart your local relay to get /openclaw/rewrite.
  - The wand appears in channel composers too (same local-only pinning — stricter than required, simpler to reason about).

## 2026-07-08T18:40:00+02:00 — T-12: PASS (LiveKit path)
- What changed: Basic audio device settings. New **Settings → Calls & audio** page (`/settings/calls`, Headphones nav icon): mic + speaker pickers via `enumerateDevices` (with an explicit "Allow device access" button when labels are permission-hidden, devicechange listener + manual rescan), and the three processing toggles (echoCancellation / noiseSuppression / autoGainControl) using the Stack `Toggle`. Persisted in new `useAudioSettings` store (zustand/persist, `gossip-audio-settings`). **Applied to calls** via CallPage → `LiveKitRoom options`: `audioCaptureDefaults` (deviceId + the three constraints) and `audioOutput.deviceId` (LiveKit routes through `setSinkId`); browsers without setSinkId get an honest disabled state with explanation. Settings take effect on the next call joined (room options are read at connect).
- Files: apps/web/src/stores/useAudioSettings.ts, pages/settings/CallSettings.tsx (new); SettingsLayout.tsx (nav), app/router.tsx (route), pages/CallPage.tsx (options wiring)  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔/eyeball (dev boots; `/settings/calls` 200; **needs human eyeball:** device dropdowns populate after permission, toggles persist across reload, next call uses picked mic/speaker) A3 ✔ (HEAD unchanged; vendor clean) A4 ✔ | B(T-12) ✔ — setSinkId used where supported (capability-detected, not faked)
- Notes / follow-ups / flags for Daniel:
  - Settings apply at room connect ("active/next call" — the next call, matching the acceptance wording). Mid-call device switching stays available via LiveKit's own device menu in the prebuilt UI.
  - Firefox lacks setSinkId on some versions — page shows the honest fallback text there.

## 2026-07-08T19:30:00+02:00 — T-13: PASS (channels) / DM sub-part BLOCKED as predicted
- What changed: Inline attachments for **channels**. **Relay:** `POST /uploads?name&type` (raw body, 10 MB cap → clean **413 with a clear error** — first version killed the socket before the 413 could arrive; fixed by draining) and `GET /uploads/:id` (files stored by server-generated uuid only — no user paths; **only png/jpeg/gif/webp/avif served inline**; HTML, SVG (script-capable), and everything else forced to `application/octet-stream` + `Content-Disposition: attachment` + nosniff, so uploads can't execute in the app origin). `post` accepts an attachment **ref**; the relay only honors ids it actually stored (**forged ids are dropped** — verified). **Client:** `lib/uploads.ts` (client-side size check + upload), `AttachmentView` (image preview / branded file card with name + human size + download), `ChannelMsg.attachment`, `post(..., attachmentId)`, Composer `onAttach` prop → ChannelView uploads each picked file then posts it (progress + error toasts); ThreadPanel renders attachments too; `/uploads` added to the Vite proxy. **DMs: cleanly deferred** — gossip-sdk has no attachment API (FINDINGS §2); the DM composer's Attach shows "Attachments aren't available in E2E DMs yet — the Gossip SDK doesn't support them." No clear-text file is ever sent around the E2E boundary; no fake success. Files mini-app NOT started (out of scope).
- Files: services/relay/server.mjs, apps/web/vite.config.ts, apps/web/src/lib/uploads.ts + components/chat/AttachmentView.tsx (new), stores/useRelay.ts, components/chat/Composer.tsx, RealDmView.tsx, ThreadPanel.tsx, pages/ChannelView.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔ A3 ✔ (HEAD unchanged; vendor clean; E2E boundary untouched) A4 ✔ | **B(T-13) ✔ verified E2E against a throwaway relay:** png upload→inline ✓ · evil.html → forced download ✓ · svg → forced download ✓ · 11 MB → 413 + clear error ✓ · WS post carries server-verified attachment ✓ · forged attachment id dropped ✓
- Notes / follow-ups / flags for Daniel:
  - Restart the local relay. Fly volume: uploads live in `$DATA_DIR/uploads` (persists like .data.json).
  - Upload has no auth (same trust level as posting — the relay doesn't authenticate posts either). Worth revisiting when relay auth lands.
  - UI eyeball: image preview sizing, file card, upload toasts.

## 2026-07-08T20:20:00+02:00 — T-14: PASS (needs human call-through)
- What changed: **Persistent calls + Discord-style dock** — the architectural task. New global call store `useCall` owns the LiveKit `Room` at **module scope, above the router**: `connect()` is idempotent per target (absorbs StrictMode double-effects), joining a different target tears the old room down first (**exactly one active session**), `RoomEvent.Disconnected` resets state on every exit path, `leave()` disconnects (stops all local tracks → mic light off). Room options include the T-12 audio prefs. **CallPage** no longer owns the connection — it fetches config/token only when the store isn't already live on its target, then binds UI via `RoomContext.Provider` + `VideoConference`; if the call ends while on the page it bounces back to the conversation. **CallDock** (new) is mounted in main.tsx **beside** `RouterProvider` — outside the router entirely — so it exists on every route incl. settings: live pill + target name ("click to return", navigates via the router instance), mic/cam/screenshare toggles (synced from room events both ways — LiveKit's own control bar updates the dock), red leave. Crucially the dock mounts a **RoomAudioRenderer** on non-call routes so remote audio keeps playing everywhere; on the call page both dock + renderer step aside (VideoConference has its own renderer — avoids doubled audio).
- Files: apps/web/src/stores/useCall.ts, components/CallDock.tsx (new); pages/CallPage.tsx (rewritten to bind, not own), main.tsx (dock mount)  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔ (dev boots, all new modules transform, styleguide untouched) A3 ✔ (HEAD unchanged; vendor clean) A4 ✔ | B(T-14) **structurally satisfied; needs human call-through** — start a call, navigate channel→DM→settings (audio should continue, dock visible + working), return via dock, leave from both dock and in-call button (mic light must go off), remount call page repeatedly (no duplicate session — connect() is target-idempotent). I can't drive mic/camera hardware from this loop, so these are flagged "needs human eyeball" per CHECKUP A2 rather than claimed.
- Notes / follow-ups / flags for Daniel:
  - Token TTL is 2h (relay-side) — a call surviving navigation is fine, but a *reconnect* after 2h needs a fresh token (LiveKit auto-reconnect uses the original token). Edge case, noting it.
  - The dock hides itself on the call page by exact path match with the active target.

## 2026-07-08T21:10:00+02:00 — T-15: PASS (DSP verified in simulation; audible check needs a human ear)
- What changed: Advanced audio — **noise gate + gain + VAD** on the outgoing track, LiveKit path. **Worklet** (`public/worklets/noise-gate.worklet.js`): per-block RMS→dB, threshold gate with 150 ms hold + smoothed attack/slow release (no clipped word tails), post-gate gain, throttled `{speaking, levelDb}` VAD posts. **Processor** (`lib/audioProcessing.ts`): LiveKit `TrackProcessor` — `getUserMedia track → MediaStreamSource → AudioWorkletNode → MediaStreamDestination → processedTrack`, attached via `track.setProcessor()` on the mic publication after connect; detached with `stopProcessor()`; live param updates via the worklet port (no reconnect). **Store** (`useAdvancedAudio`, persisted: enabled/threshold/gain; runtime VAD not persisted). **UI**: CallSettings "Advanced — noise gate" group: enable toggle (attaches/detaches mid-call), threshold slider (−70…−20 dB), gain slider (−10…+12 dB), and a live **"voice detected" VAD pill** with level readout during an active call. `useCall` wires attach-on-connect and reset on every disconnect path. **DSP verified by simulation** (worklet run in node with stubbed WorkletGlobals): −60 dB noise → output 0.00000 (gated ✓); −13 dB signal at +6 dB gain → 0.3 → 0.59 peak (passes, amplified ✓).
- Files: apps/web/public/worklets/noise-gate.worklet.js, src/lib/audioProcessing.ts, src/stores/useAdvancedAudio.ts (new); stores/useCall.ts, pages/settings/CallSettings.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck 0, build ✓) A2 ✔ (worklet served at /worklets/…, settings page 200, styleguide untouched) A3 ✔ (HEAD unchanged; vendor clean; processing is 100% local — nothing leaves the device) A4 ✔ | B(T-15) ✔ gate+gain verified mathematically; **audible** outgoing-track check + VAD pill during a real call needs your ear
- Notes / follow-ups / flags for Daniel:
  - The gate applies to the **outgoing** mic track only (as specced) — peers hear the gated audio, your local monitoring is unaffected.
  - Chrome's echoCancellation (T-12 toggle) runs *before* the worklet, which is the right order.

## ALL TASKS COMPLETE OR BLOCKED
All 16 backlog tasks are `[x]` (T-13's DM sub-part is the one documented in-task block: no SDK attachment API — noted inline on the checkbox). Nothing was committed or pushed; the entire batch sits uncommitted in the working tree for your local test pass (see RALPH.md §6 for the review/commit flow). Restart the relay before testing (new ops: editMessage/deleteMessage, /openclaw/rewrite, /uploads).

## 2026-07-14T13:29 — T2-00: PASS
- What changed: Appended "Batch 2 recon" to ralph/FINDINGS.md answering all nine T2-00 bullets with real paths: em-dash copy-vs-keep classification, tooltip primitive ABSENT, the two code formatters (MessageBody.tsx fenced blocks with no highlight/copy; AiPage/AiSidePanel raw whitespace-pre-wrap AI output), back-button gaps (/settings/* origin bug, MiniAppHost, /dev/*), channel model (relay joinChannel has NO membership check; private = cosmetic), roles (owner/member only, one isAdmin gate at server.mjs:570), no mention parsing, no notification infra (event sources listed; no call-start signal exists), call UI = LiveKit prefab VideoConference with default theme and no in-call chat.
- Files: ralph/FINDINGS.md, ralph/RALPH-2.md (checkbox)  (left UNCOMMITTED in the working tree)
- Checkup: A1 ✔ (no code changed; typecheck+build passed at f6859e8 baseline earlier today) A2 ✔ (no product change) A3 ✔ (no commit/push, HEAD f6859e8; diff ralph/ only) A4 ✔ | B(T2-00) ✔
- Notes for Daniel: batch-1 T-07 confirmed done, so T2-06 will target layout/dark-mode/chat only. T2-07/T2-08 enforcement gap is real: relay joinChannel (server.mjs:502) checks nothing.

## 2026-07-14T14:26 — T2-01: PASS
- What changed: Hit-by-hit rewrite of every user-visible em dash from the FINDINGS classification: 26 strings across 16 files (sidebars, call dock, composer notice, DM/channel empty states + placeholders, search/AI privacy notes, settings copy, onboarding tagline). Each replaced with a comma, colon, period, parens, or middot as the sentence wanted; no sed, no code/CSS/deps/docs touched.
- Files: DmSidebar, CallDock, Composer, NewDmDialog, RealDmView, AiPage, ChannelView, HomePage, SearchPage, IdentityCreate, OnboardingLayout, AiEngineSettings, CallSettings, IntegrationsSettings, ProfileSettings, SecuritySettings (all apps/web/src; left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck + build pass) A2 partial (string-only JSX changes; visual read-through needs human eyeball) A3 ✔ (no commit/push, HEAD f6859e8; no vendor/sdk/wasm; no secrets) A4 ✔ | B(T2-01) ✔ grep now returns only documented keeps: shareText quote-attribution dashes (RealDmView:185, ChannelView:206, intentional typography), bare placeholder values (AiEngineSettings:80, WorkspaceSettings:19/47, mock.ts:134), CSS/code comments, unrendered mock fixtures, dev-only /dev/* pages, packages/ui docstrings, Styleguide en-dash date range.
- Notes for Daniel: kept the em dash in copied-to-clipboard quote attribution on purpose; say the word and I'll swap those too.

## 2026-07-14T14:34 — T2-02: PASS
- What changed: New Stack tooltip primitive (packages/ui/src/stack/Tooltip.tsx, exported from @gossip/ui/stack): hover after 350ms + immediate on keyboard focus, Esc/blur dismiss, aria-describedby on the trigger while visible, role=tooltip, inverse-ink bubble with 4 sides + 120ms fade (keyframes in tokens.css). Swept it across every icon-only control, replacing native title=: composer toolbar (AI/bold/italic/code/code-block/emoji/plus/send/dismiss), message actions (copy/share with live Copied! label, edit pencils in channel+DM, two-step delete), pane-header buttons (side=bottom, covers DM call button), CallDock controls + leave, workspace rail (home/workspaces/create/mini-app dock/settings/avatar, side=right), sidebar + add buttons (new addLabel prop), HomePage row actions, NewDmDialog copy, AiSidePanel close.
- Files: packages/ui/src/stack/{Tooltip.tsx,index.ts,tokens.css}; apps/web/src: Composer, MessageActionsBar, PaneHeader, CallDock, WorkspaceRail, ChannelSidebar, DmSidebar, ChannelView, RealDmView, HomePage, NewDmDialog, AiSidePanel  (left UNCOMMITTED)
- Checkup: A1 ✔ (web + ui typecheck, build pass; no dep changes) A2 needs human eyeball (hover/focus/Esc behavior + placement; tokens.css addition is additive-only so styleguide look unchanged) A3 ✔ (no commit/push, HEAD f6859e8; no vendor/sdk/wasm; no secrets; primitive lives in packages/ui per boundary) A4 ✔ (T2-01 work untouched) | B(T2-02) ✔ hover+focus+Esc implemented, labels accurate, works in DM/channel/settings surfaces
- Notes for Daniel: emoji/plus outside-click toggles verified intact (closest() targets the buttons, not the wrapper). Text-bearing buttons (Reply, CallDock return, invite-copy) intentionally keep native behavior. Optional follow-up: add a Tooltip demo block to /dev/styleguide.

## 2026-07-14T14:44 — T2-03: PASS
- What changed: Shared BackButton (apps/web/src/components/BackButton.tsx: explicit parent route, ArrowLeft icon, aria-label + T2-02 tooltip) and a back slot on PaneHeader. MiniAppHost (/w/:id/apps/:appId) now has a back control to the mini-app launcher. Settings back link is origin-aware: rail/apps/mini-app links pass state.from, SettingsLayout captures it once at mount (survives tab switches, validated as an internal path), and the fallback chain is first-workspace then /home instead of /welcome; the label follows the target (Back to workspace / Back to messages).
- Files: components/BackButton.tsx (new), components/chat/PaneHeader.tsx, pages/MiniAppHost.tsx, pages/AppsPage.tsx, pages/settings/SettingsLayout.tsx, app/WorkspaceRail.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck + build pass) A2 needs human eyeball (back placement + fresh-deep-link landing) A3 ✔ (no commit/push, HEAD f6859e8; no vendor/sdk/wasm; no secrets) A4 ✔ | B(T2-03) ✔ flagged routes covered; fresh deep-link to /settings/* lands on first workspace or /home, /w/:id/apps/:appId lands on the launcher
- Notes for Daniel: /dev/styleguide and /dev/sdk-smoke intentionally skipped (dev-only tooling, and the styleguide is the visual regression guard). CallPage in-call stays back-free by design (CallDock is the way back).

## 2026-07-14T14:56 — T2-04: PASS
- What changed: Consolidated on Shiki. New lib/highlight.ts (lazy createHighlighterCore singleton: JS regex engine, github-light/dark dual themes as CSS vars, 14 grammars, alias map, 200-entry render cache; all dynamic imports so grammars land in code-split chunks — main bundle +~7KB). New components/chat/CodeBlock.tsx: themed container (header with language label + one-click Copy of the exact raw source with Copied state + tooltip), async highlight with inert plain-text fallback. MessageBody now renders fences through CodeBlock (root fix for chat formatter #1); AI surfaces (AiPage turns, AiSidePanel answers) now render through MessageBody instead of raw whitespace-pre-wrap (root fix for formatter #2) — inline code stays as the distinct md-chip. Dark-mode flip via .dark .code-block-body CSS reading --shiki-dark vars (index.css).
- Files: apps/web/src/lib/highlight.ts (new), components/chat/CodeBlock.tsx (new), components/chat/MessageBody.tsx, pages/AiPage.tsx, components/chat/AiSidePanel.tsx, index.css, apps/web/package.json + pnpm-lock.yaml (add shiki)  (left UNCOMMITTED)
- Checkup: A1 ✔ (pnpm install clean, typecheck + build pass; shiki grammars code-split) A2 needs human eyeball (visual: highlight colors light/dark, copy button) A3 ✔ (no commit/push, HEAD f6859e8; lockfile churn intentional for shiki; no vendor/sdk/wasm; no secrets) A4 ✔ | B(T2-04) ✔ XSS verified by direct node run: shiki emits &#x3C; for < — '<img src=x onerror>' inside a fence renders inert; copy copies raw source; inline vs block distinct; DM + channel + threads + AI all share the same path
- Notes for Daniel: languages loaded: ts/tsx/js/jsx/json/bash/python/rust/go/html/css/sql/yaml/markdown (aliases js/ts/sh/py/rs/yml/md/golang). Unknown languages render plain with the copy button, no fake highlight.

## 2026-07-14T15:04 — T2-05: PASS
- What changed: @mention autocomplete in the shared Composer. Typing @ (at start or after whitespace) opens a branded MentionPopover (new component: listbox semantics, avatars + mono handle, 8-result cap, st-pop-in entrance animation) that filters live; ArrowUp/Down cycle, Enter/Tab insert, Esc closes, click selects (mousedown-preventDefault keeps textarea focus); textarea gets aria-autocomplete/expanded/controls/activedescendant. Selecting inserts a structured token @[Display](gossip1…id) — plain text, transport-agnostic, E2E-safe — and MessageBody renders it as a highlighted md-chip carrying data-mention-id for T2-09 notification routing. Candidates: workspace members in channels (useRelay), contacts in DMs (useContacts). No pre-existing @ parsing to unify (FINDINGS B2-7).
- Files: components/chat/MentionPopover.tsx (new), Composer.tsx, MessageBody.tsx, index.css (st-pop-in), pages/ChannelView.tsx, components/chat/RealDmView.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck + build pass) A2 needs human eyeball (popover look, keyboard feel) A3 ✔ (no commit/push, HEAD f6859e8; message storage stays plain text — no transport changes; no vendor/sdk/wasm; no secrets) A4 ✔ | B(T2-05) ✔ regex round-trip verified by node run: mid-word @ never triggers, bare @ lists everyone, token parses display+id, non-gossip1 ids render inert
- Notes for Daniel: display names are sanitized ([]() stripped) before tokenizing; exit animation omitted (only an entrance fade/rise — exit needs unmount deferral, not worth it here). ThreadPanel still has its own bare input (not the shared Composer), so no mentions there yet.

## 2026-07-14T15:10 — T2-06: PASS (needs human call-test)
- What changed: Replaced LiveKit's prefab <VideoConference /> with a composed call surface. New components/call/CallStage.tsx: GridLayout + ParticipantTile keep LiveKit's responsive tile grid / speaking ring / mute badges (data-lk-theme scoped to the grid only now), width capped (max-w-3xl for 1:1, max-w-5xl beyond) so tiles stay sane; control tray is ours on Stack tokens — mic/cam show negative fill when off, screenshare/chat invert when engaged, leave is a wide negative button — no more white-on-dark lk defaults. New components/call/CallChatPanel.tsx: in-call text chat that is NOT a new transport — channel calls dock the channel's own relay chat (joinChannel + post + live store), DM calls dock the E2E conversation via gossip-sdk (getVisibleMessages/sendText + SDK events); shared Composer with mentions/markdown, MessageBody rendering, auto-scroll, toggle from the tray. RoomAudioRenderer moved into CallStage (CallDock still covers every other route; mutually exclusive so no double audio).
- Files: components/call/CallStage.tsx (new), components/call/CallChatPanel.tsx (new), pages/CallPage.tsx, components/CallDock.tsx (comment)  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck + build pass) A2 NEEDS HUMAN EYEBALL/CALL-TEST: join a real call and check grid reflow, dark-mode tray, chat send/receive both branches, audio continues, dock handoff on navigate (batch-1 T-14 path untouched but re-verify) A3 ✔ (no commit/push, HEAD f6859e8; DM chat stays inside gossip-sdk, room naming untouched; no vendor/sdk/wasm edits; no secrets) A4 ✔ | B(T2-06) static parts ✔; T-07 header/branding preserved
- Notes for Daniel: chat panel defaults open; the tray chat button toggles it. Channel-call chat posts land in the real channel (that is the point); DM-call chat is the real E2E thread. Speaking indicator/mute badges come from ParticipantTile. If tiles look off, the lk default theme CSS is still loaded and scoped to the grid container.

## 2026-07-14T15:28 — T2-07: PASS (relay-enforced, 14/14 automated checks)
- What changed: Real roles/permissions/bans, enforced at the relay per Batch-2 guardrail #1. Relay (services/relay/server.mjs): members carry role owner|admin|member + granular admin permissions (manageChannels, manageMembers, manageRoles, ban, moderateMessages) chosen by the Owner; workspace.bans records {userId,name,reason,by,at}; new ops setRole (owner-only, bogus perms filtered), banMember (owner/admin-with-ban; can't ban owner/self; kicks live sockets + notifies), unbanMember. Enforcement added to joinWorkspace/openWorkspace/joinChannel/post/editMessage/createChannel (bans) and deleteMessage-others now requires moderateMessages instead of blanket admin. Client (useRelay): AdminPermission types + ADMIN_PERMISSIONS catalog, bans on RelayWorkspace, setRole/banMember/unbanMember actions, live memberUpdated/memberBanned/memberUnbanned handling, and a banned handler that drops the workspace locally when YOU are banned. MembersPage: role badges with permission tooltips, owner-only Make admin / Edit permissions / Remove admin (permission-checkbox modal), Ban with reason dialog, Banned section with Unban, honest footnote that relay identity is self-reported (governance, not cryptographic auth).
- Files: services/relay/server.mjs, apps/web/src/stores/useRelay.ts, apps/web/src/pages/MembersPage.tsx  (left UNCOMMITTED; data model additive — existing relay-data.json loads unchanged)
- Checkup: A1 ✔ (typecheck + build pass; node --check on relay) A2 needs human eyeball (MembersPage UI) A3 ✔ (no commit/push, HEAD f6859e8; no vendor/sdk/wasm; no secrets) A4 ✔ | B(T2-07) ✔ VERIFIED against a scratch relay instance (port 8933, throwaway data dir): 14/14 automated checks — non-owner setRole rejected, owner promote with bogus-perm filtering, admin-with-ban bans, target kicked+notified, banned rejoin/open/joinChannel rejected, banned post dropped from history, owner unbannable, demoted admin loses unban, owner unban restores rejoin.
- Notes for Daniel: RESTART THE RELAY before testing (new ops). Enforcement is real at the transport but identity is the self-reported hello handle (pre-existing relay design) — flagged in the UI footnote. manageRoles is reserved (assignment stays owner-only per the task); manageChannels gets teeth in T2-08.

## 2026-07-14T15:44 — T2-08: PASS (relay-enforced, 16/16 automated checks)
- What changed: Private channels are genuinely invite-only, enforced at the relay. Relay: private channels carry a member map (creator seeded at creation); per-requester workspace serialization HIDES private channels from non-members (they can't even see they exist); joinChannel/post/editMessage/deleteMessage all gate on channel membership; channelCreated for private goes to the creator only; new ops addChannelMember (creator or manageMembers; target must be a workspace member) and removeChannelMember (managers, or yourself to leave; creator only removable by self) with live channelUpdated/channelRemoved fan-out + kick of the removed user's subscriptions; gatherChannelContext now SKIPS private channels entirely (the AI jobs endpoint is unauthenticated, so recaps could otherwise leak private content); additive boot migration grandfathers all current workspace members into pre-T2-08 private channels so nobody silently loses access. Client: RelayChannel.members, channelUpdated/channelRemoved handling (removal drops channel+messages+sub), addChannelMember/removeChannelMember actions. UI: ChannelView private header button opens the new ChannelMembersDialog (roster, invite from workspace, remove/leave, creator marked; UX-gated, relay-enforced), honest banner 'Invite-only private channel (relay-enforced). Messages are workspace-confidential, not E2E yet.', and a 'Channel unavailable' state for links to channels you can't see.
- Files: services/relay/server.mjs, apps/web/src/stores/useRelay.ts, apps/web/src/pages/ChannelView.tsx, apps/web/src/components/chat/ChannelMembersDialog.tsx (new)  (left UNCOMMITTED; migration additive)
- Checkup: A1 ✔ (typecheck + build pass; node --check relay) A2 needs human eyeball (dialog UI, invited-channel appearing live in sidebar) A3 ✔ (no commit/push, HEAD f6859e8; honest privacy labels; no vendor/sdk/wasm; no secrets) A4 ✔ | B(T2-08) ✔ VERIFIED against a scratch relay: 16/16 — creator-only seed, snapshot hiding, join/post rejection incl. pre-invite sneaky post absent from history, non-manager invite rejected, invite delivers channel live, member read/post works, removal kicks stream + blocks rejoin, self-leave, creator irremovable by others, owner manages channels they didn't create.
- Notes for Daniel: restart the relay (new ops + migration). AI recaps now exclude private channels BY DESIGN until the jobs endpoint learns requester identity — mention it if someone asks why #secret isn't in a recap. Sidebar + search show only channels the relay serves you, so no client changes were needed there.

## 2026-07-14T16:03 — T2-09: PASS (relay signal verified 6/6; UI needs human eyeball)
- What changed: Full in-app notification layer. New stores/useNotifications.ts: inbox (cap 100, session-scoped) with read/unread, per-channel + per-DM unread counters, localStorage prefs (per-type toggles, per-channel/DM mutes, OS, sound), suppression rules (own messages, muted, and the conversation currently on screen), WebAudio beep (no assets), OS Web Notifications only when tab hidden + permission granted. Event sources wired: relay messages (T2-05 mention tokens promote to 'mention' type with their own toggle), workspace joins, you-were-added-to-private-channel, and a NEW relay callStarted broadcast (first LiveKit token per channel room in 5min announces; joiners don't re-announce; private rooms announce to members only; DM rooms never broadcast — no metadata leak). DM wiring (lib/useDmNotifications.ts, mounted in both shells): notifications are deliberately CONTENT-FREE ('New encrypted message from X'), so DM content never even reaches OS notification surfaces. UI: NotificationToaster (global, outside router, click deep-links), NotificationCenter (rail bell + inbox panel: unread badge, mark-all-read, per-item deep links, settings shortcut), unread pills on channel rows / DM rows / the rail home button (DM total), viewing a conversation clears its counter, and NotificationsSettings replaces the placeholder with real toggles + mute lists.
- Files: stores/useNotifications.ts (new), lib/useDmNotifications.ts (new), components/NotificationToaster.tsx (new), components/NotificationCenter.tsx (new), stores/useRelay.ts, services/relay/server.mjs, app/{AppShell,HomeShell,WorkspaceRail,ChannelSidebar,DmSidebar}.tsx, pages/ChannelView.tsx, components/chat/RealDmView.tsx, pages/settings/NotificationsSettings.tsx, main.tsx  (left UNCOMMITTED)
- Checkup: A1 ✔ (typecheck + build pass; node --check relay) A2 needs human eyeball (toasts, bell panel, badges, OS permission flow, sound) A3 ✔ (no commit/push, HEAD f6859e8; DM bodies never leave the SDK store — notification store is local state, DM notifications carry no content; check the diff: nothing ships DM bodies to any transport/log/model) A4 ✔ | B(T2-09) relay callStarted VERIFIED 6/6 against a scratch relay (announce, joiner debounce, private-member-only fan-out, DM-room silence); client store logic is typechecked + code-reviewed, runtime flows flagged for the human pass
- Notes for Daniel: restart the relay (callStarted). Inbox is session-scoped by design (prefs persist, items don't) — say the word if you want it persisted. OS notifications fire only when the tab is hidden; enable them in Settings → Notifications.

## ALL TASKS COMPLETE OR BLOCKED
Batch 2 is done: T2-00 through T2-09 all [x], none blocked. Nothing was committed or pushed — the entire batch sits uncommitted in the working tree for your local test pass (restart the relay first: T2-07 roles/bans ops, T2-08 private-channel enforcement + migration, T2-09 callStarted). Review with git diff, test, commit yourself per RALPH.md §6.
